# Local environment artifacts
.claude/
.vscode/
.idea/
.zed/
*.code-workspace

# OS noise
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Log + temp
*.log
*.tmp
*.swp
*.swo
*~

# Editor-saved backups
*.bak
*.orig

# WordPress local-preview install (lives at ../wp/, but defensive guard)
wp/
wordpress/
wp-latest.zip
wp-config.php

# Build / dependency artifacts that should never ship in this bundle
node_modules/
vendor/
composer.lock
package-lock.json
yarn.lock

# Secrets — never commit any of these
.env
.env.*
*.pem
*.key
showtime-pools-core/secrets.php
showtime-pools-core/.htaccess.local

# ACF JSON cache (we keep the canonical group_*.json, ignore any auto-generated)
showtime-pools-child/acf-json/cache/

# Build outputs
dist/
build/
*.zip

# Database dumps — never commit DB content (WP DB is the live source of truth)
*.sql
*.sql.gz

# Folder-snapshot checkpoints (fallback when git tags aren't enough)
.checkpoints/

# PHP test runner cache
.phpunit.result.cache

# Plan files written by AI tooling (live in user-scoped paths, defensive)
plans/
